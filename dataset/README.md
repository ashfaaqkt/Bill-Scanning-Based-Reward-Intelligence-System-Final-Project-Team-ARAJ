# Dataset

## Sources & Status

| Source | Count | Owner | Stored At | Status |
|---|---|---|---|---|
| CORD (Clova OCR) | ~1000 | Arpan | `dataset/genuine/` | ⚠️ NOT YET uploaded to Drive |
| SROIE (ICDAR 2019) | ~973 | Arpan | `dataset/genuine/` | ⚠️ NOT YET uploaded to Drive |
| Indian receipts (Jyoti raw) | 50 | Jyoti | `dataset/indian/` | ⚠️ NOT YET uploaded to Drive |
| Tampered (generated) | 151 labelled | Ranjeet | `dataset/tampered/` | ✅ Labels in labels.csv; images via generate_tampered.py |
| Test bills | 5 | Ashfaaq | `dataset/test_bills/` | ✅ In repo (used for OCR testing) |

> **Action needed:** Arpan to upload CORD + SROIE images. Jyoti to upload 50 raw Indian receipt photos.
> Shared Google Drive: https://drive.google.com/drive/folders/10i_o-jpTaQv2Zk2jJVRCUc3muzCGLB8Z

## Folder Structure

```
dataset/
  genuine/          ← CORD + SROIE images go here (gitignored, download from Drive)
  indian/           ← Jyoti's 50 raw receipt photos go here (gitignored, download from Drive)
  tampered/         ← Generated by generate_tampered.py (gitignored)
  test_bills/       ← 5 sample bills for OCR smoke testing (in repo)
  processed/
    labels.csv      ← 151 labelled samples (Ranjeet + Jyoti)
    receipts_master.csv ← Full feature CSV for ML training (populate after downloading images)
  labels.csv        ← Jyoti's standalone label file (50 rows)
  generate_tampered.py   ← Creates tampered variants (4 modes: brightness, duplicate_copy, number_overwrite, handwritten)
  exact_duplicate_check.py ← Flags exact duplicate receipts from CSV
  perceptual_hash.py     ← pHash image similarity comparison tool
  fraud_detector.py      ← Standalone multi-signal fraud scorer
```

## processed/receipts_master.csv Schema

```
image_path, merchant, date, total, category, items_text, source
```

## processed/labels.csv Schema

```
image_path, label, labelled_by, notes
```

Labels: `genuine` / `tampered` / `blurry` / `multi_bill` / `handwritten`

## Note

Raw images are NOT committed to Git (too large — gitignored).
Only CSV files are version-controlled.
All images must be downloaded from the shared Google Drive before running any notebook.
